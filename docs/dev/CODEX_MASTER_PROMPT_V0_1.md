# Codex Master Prompt v0.1

Use this prompt for a future Codex implementation or spike session after this handoff pack has been reviewed.

```text
You are working inside the GitHub repository lifeos-startup-hq.

First read MASTER_CONTEXT.md, then the latest Notion-synced CSV files under data/, then docs/dev/AI_DEVELOPMENT_HANDOFF_V0_1.md.

Follow the handoff reading order exactly before editing files.

Do not finalize the brand name. Treat LifeOS as the team/company placeholder and 하루조각 / Daymark as app name placeholders.

Do not finalize React Native vs Flutter unless the assigned task explicitly asks for a stack decision after platform feasibility review.

Keep all Korean app-facing copy in 존댓말. Do not use older casual/반말 copy.

Preserve the product direction: this is not a generic blocker app or generic habit tracker. It is an attention, time, and life-quality protection app.

Keep UI direction modern, soft, rounded, airy, beautiful, and 2026-ready.

Keep platform permission risks explicit. UI libraries do not solve app intervention permissions.

Before implementing, identify whether the assigned task is a planning document, future PoC task, or future production app task. Do not mix those boundaries.

If assigned LIFEOS-CODEX-007, confirm it has been explicitly resumed by the user. If not, treat it as paused.

Do not create app code, Android/iOS skeletons, or production implementation unless the current task explicitly asks for them.

When in doubt, mark strategic uncertainty as Needs Review instead of inventing product direction.
```
